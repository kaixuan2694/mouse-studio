# 指针工坊 · Mouse Studio

双击 **MouseStudio.exe** 即可使用。Windows 原生桌面程序，无需安装，无需管理员权限，不需要网络。面向 Windows 10 / 11，使用系统 .NET Framework 4.x。

## 三个主要功能

1. **选择样式**：点击卡片立即应用到系统鼠标，共两页，每页 10 款。第一页：素笺、藏锋、流光、桃夭、听竹、方寸、逐日、渡海、鎏月、星游。第二页：游龙、折纸、飞羽、长剑、团扇、小鱼、猫步、玉簪、山岚、火箭。翻页不会改变已选指针。
2. **调整大小**：滑动选择 24–96 px 的指针画布尺寸，约 100 毫秒后应用。未选风格时，首次拖动使用素笺。实际可见图形略小于画布。
3. **调整灵敏度**：滑动选择 Windows 鼠标速度 1–20，实时生效。程序读取当前系统速度作为初值，不更改鼠标加速选项。此项是系统移动速度，不是硬件 DPI。

除普通箭头外，文字选择、链接手形、忙碌、精确选择、上箭头、四种缩放、移动、禁止、后台工作、帮助等共 14 种系统指针状态也会同步改变颜色和尺寸。忙碌状态在此版本使用静态圆环。

## 独立配色

点击每款指针右侧的小色块，选择预设色或“自选颜色…”。配色只影响这一款指针，自动保存；如果正在使用此款，会立即更新系统指针颜色。选择“恢复这款默认配色”可还原该款颜色。描边自动保留明暗对比，部分装饰保留点缀色。窗口的“恢复原设置”只恢复系统鼠标，不清除保存的配色。

## 托盘与恢复

- 点击窗口右上角 × 会隐藏至任务栏通知区域，效果继续生效。图标可能收在“显示隐藏的图标”中。
- 双击绿色指针托盘图标重新打开窗口。
- 窗口内的“恢复原设置”重新加载 Windows 已保存的指针主题，并恢复程序启动时的鼠标速度；不再恢复可能已经异常的临时光标副本。其他软件未保存的临时指针效果不会被保留。
- 托盘图标右键 → “退出并恢复”会还原设置并完全退出。
- 窗口内按 `Ctrl+Q` 也可退出并恢复。
- 退出时还原系统主题与速度，但保留上次配置，下次打开自动应用。点击窗口中的“恢复原设置”则清除自动应用当前配置的状态，直到再次选择样式或调整滑块；各款配色仍保留。
- 如果被任务管理器强制结束，重新打开程序会先恢复 Windows 已保存的指针主题以及备份速度。也可在程序未运行时双击“恢复系统鼠标.cmd”。异常恢复时使用系统已保存的主题，无法保留其他程序尚未保存的临时指针效果。

部分软件会自行绘制鼠标指针，或自行处理原始鼠标输入，因此可能不跟随 Windows 的指针样式或速度。切换 Windows 主题、辅助功能指针设置也可能覆盖当前效果，此时重新点击风格即可。

## 开机自启与自动记忆

1. 首次使用新版，选择所需样式、颜色、大小和速度，配置会自动保存。
2. 勾选底部“开机自启，自动应用上次配置”，下次登录当前 Windows 账号时，程序会在托盘运行并应用上次配置。
3. 取消勾选会移除本程序的当前用户启动项，不影响当前效果。开机自启默认关闭。
4. 手动打开程序也会自动恢复已保存配置。“退出并恢复”保留配置，“恢复原设置”停止自动应用当前配置。
5. 请把 EXE 放在固定位置。如果移动了程序，重新手动打开一次可更新已启用的启动路径。如果 Windows 的“启动应用”禁用了该项，请在系统中重新启用。

开机自启通过当前用户的 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\MouseStudio` 注册，无需管理员权限。实际触发时机是用户登录 Windows，不是在登录之前。

## 窗口大小

拖动窗口边缘或最大化后，卡片和滑块会随窗口宽高重新布局。较小窗口提供纵向滚动，可滚动到开机选项和恢复按钮。

## 文件

- `MouseStudio.exe`：独立运行文件，所有风格均内置。
- `MouseStudio.cs`：完整 C# 源码。
- `Preferences.cs`、`ResponsiveLayout.cs`：配置、自启和自适应布局代码。
- `BehaviorTests.cs`：配置及界面回归测试。
- `build.ps1`、`app.manifest`：本机编译脚本和应用清单。
- `preview.png`：真实窗体渲染预览。
- `test-results.txt`：本机自动验证结果。

程序在 `%LOCALAPPDATA%\MouseStudio\recovery.txt` 保存恢复所需的初始速度标记，正常恢复后删除；`colors.txt` 保存每款指针的自选配色；`profile.txt` 保存当前样式、大小、速度及是否自动应用。不采集鼠标轨迹。

## 开发和验证

PowerShell 中运行 `./build.ps1` 编译。使用 Windows 自带的 .NET Framework C# 编译器，不依赖 NuGet。

`MouseStudio.exe --self-test` 会短暂改变真实系统鼠标，验证后在 finally 中恢复。覆盖 20 风格 × 3 尺寸 × 14 系统状态的尺寸和热点，速度 1/10/20 的系统读回，以及原指针图像、掩码、热点的 SHA-256 比对。另测了两页导航、选中状态保持、独立配色及系统颜色更新、尺寸滑块延迟应用、速度滑块、恢复、关闭到托盘、重新打开和退出。

`MouseStudio.exe --preview` 生成界面预览，不应用鼠标设置。

系统接口依据：

- [SetSystemCursor（Microsoft）](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setsystemcursor)
- [SystemParametersInfo（Microsoft）](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-systemparametersinfow)
- [CreateIconIndirect（Microsoft）](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-createiconindirect)
